export interface ICourseInfo {
  total: number | string;
  title: string;
  bgColor: string;
}

export interface ICourseTopProps {
  title?: string;
  subTitle?: string;
  category?: string;
  width: string;
  type: string;
}